// TODO: Add map of routes we want to preserve the scroll position from.
export const preserveScrollRouteMap: { from: string; to: string }[] = [
  {
    from: '/products/[id]',
    to: '/products',
  },
];
