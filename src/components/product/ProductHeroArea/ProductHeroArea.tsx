import * as Styled from './styles';

// TODO: Actually write this component.
// TODO: Decide where best to locate Page specific components.
export const ProductHeroArea: React.FC<{ children: React.ReactNode }> = ({ children }) => (
  <Styled.ProductHeroArea>{children}</Styled.ProductHeroArea>
);
