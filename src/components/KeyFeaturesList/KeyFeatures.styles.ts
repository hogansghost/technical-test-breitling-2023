import styled from 'styled-components';

export const KeyFeatures = styled.div``;

export const KeyFeaturesList = styled.ul`
  display: grid;
  gap: 32px;
  grid-template-columns: repeat(auto-fit, clamp(100px, 220px, 100%));
  list-style: none;
  margin: 0;
  padding: 0;
`;
